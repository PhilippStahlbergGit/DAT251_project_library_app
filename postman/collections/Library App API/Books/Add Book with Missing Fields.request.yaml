$kind: http-request
method: POST
url: '{{baseUrl}}/api/books'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {}
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status code is 200 or 400 (no validation currently, 200 expected - known gap)", function () {
          pm.expect([200, 400]).to.include(pm.response.code);
      });
